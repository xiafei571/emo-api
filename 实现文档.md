# Agent 原始轨迹 S3 归档与训练数据构建实现文档

## 目标与边界

以每个经过鉴权的 HTTP 推理请求为最小归档单位，保存客户端请求正文、实际写给客户端的响应正文和关联元数据，供后续按用户、会话重建训练轨迹。不开启上游重复正文采集，不更改转发、计费、客户端响应或模型参数。默认关闭，通过环境变量启用；本次实现不自动配置生产凭据或发布。

首版覆盖 `/v1` 的 HTTP POST 推理接口、`/v1beta/models/*` 和 Playground 对话。包含 JSON、SSE、multipart、二进制正文；WebSocket/Realtime、异步视频/Midjourney/Suno 任务及后台轮询不属于本版 HTTP 对话归档。网关无法采集客户端未发送的本地工具执行过程。请求体是 HTTP 解压之后、业务解析/改写之前的应用层字节；响应是实际交给 HTTP writer 的应用层字节，不包含 TLS/TCP 分帧。

## S3 存储树

```text
s3://<bucket>/<prefix>/
  user=123/
    session=<sha256-of-client-session-id>/
      date=2026-09-08/
        req_<archive-uuid>.jsonl.gz
    session=unknown/
      date=2026-09-08/
        req_<archive-uuid>.jsonl.gz
```

默认 prefix 为 `raw/v1`。日期按请求开始时间的 UTC 日期；同一 session 可跨天。user_id/api_key_id 取鉴权上下文，绝不相信客户端自报 user_id。session 优先读取 `X-EMO-Session-ID`，其次 `session_id` 请求头（最多 256 字节），保存原始 ID 和来源，但对象路径使用 SHA-256，避免路径注入和原始会话 ID 外泄。缺失/非法标识归入 unknown；unknown 目录中的请求不是一个会话。不会用网站登录 session、API Key、prompt_cache_key 或时间间隔猜测会话。body 中的关联字段完整保留，清洗时可以额外解析。

archive_id 使用服务端新 UUID，request_id 使用已有网关 request_id；两者都写入元数据。后台重试上传同一 archive_id 使用固定 key，避免同一请求的归档因上传重试变成多个训练样本。客户端重试是独立 HTTP 请求，仍独立归档。

## JSONL v1

每行包含 `schema_version`、`type`、`seq`、`timestamp`；seq 在单文件内单调递增，timestamp 为 UTC RFC3339Nano。

| 事件 | 内容 |
| --- | --- |
| request.start | archive_id、request_id、user_id、api_key_id、session_id/source、method、path、content_type、声明的 content_length |
| request.chunk | 原始请求正文分片，body_encoding=base64、body_raw |
| response.chunk | 实际写出的响应正文分片，body_encoding=base64、body_raw |
| request.end | HTTP 状态、响应 content_type、模型/分组/最终渠道、上游 request_id、计费用量（若产生）、读写字节数、是否采集完整、终止原因、流终止信息（若产生） |

为避免巨大请求和二进制正文占满内存，原始请求也分片，不能假设 request.start 内含完整正文。每片最多 32 KiB 原始字节；统一 Base64 可无损保留跨 UTF-8 字符边界的字节。恢复正文时分别选择 request.chunk / response.chunk，按 seq 排序，先逐片 Base64 解码再拼接。writer 分片不等于 SSE 事件或 token；解析 SSE 必须在重组字节后执行。上传对象不设置 Content-Encoding:gzip，以便下载得到真实 `.gz` 文件。

不采集 Authorization、Cookie、API Key、完整 URL query 或任意请求头。正文仍可能含用户代码和敏感信息；bucket 应私有、加密并限制访问，训练导出前按实际授权范围筛选和脱敏。没有获得授权的事实不应被系统自动推断为训练许可。

`capture_complete` 只表示请求/响应已观察到的传输数据完整采集，不代表模型成功或对话完整。HTTP 200 的流也可能异常；通过 stream_end_reason/stream_error_count 辅助筛选，未知状态不伪造成功。usage 为计费链路给出的汇总；原始响应中的 usage 保留，失败请求可能无汇总。

## 采集和上传

1. 鉴权成功后创建私有本地 `.open` 文件并写入 start。
2. 用透明 request body reader 记录业务实际读取的请求字节，不预读、不重复记录业务重用正文；未读取完的请求明确标记。
3. 用 Gin ResponseWriter 包装实际写出的字节，保留 WriteString、Flush 和底层 ResponseController 能力。本地分片直接落文件，不缓存整段响应；正常请求不等待 S3。
4. 正常结束/取消/异常时追加 end，fsync，原子重命名为 `.ready`。
5. 固定数量后台 worker 扫描持久化队列，流式 gzip 压缩、fsync、原子重命名为 `.jsonl.gz`，随后上传。
6. PUT 成功后删除本地归档。失败文件保留，按有上限的指数退避重试；重启后继续扫描上传。

本地目录必须挂载持久卷，每实例独享，并持有进程锁。启动时恢复遗留 `.open`：移除最后不完整 JSON 行，补 process_interrupted 结束标记再入队；不把其标成完整。若 end 已完整写入、只差重命名，则保留原 end，不重复追加。无法解析 start 的非空文件保留为 `.corrupt`，需人工处理，不当作训练轨迹上传。半成品压缩文件可重建。无需向数据库写每个分片，raw 文件本身带元数据；第一版按 S3 前缀列举，不额外维护可变的 session manifest。成功上传后移除空的用户/session/日期目录，避免目录不断累积。

本地写入和 fsync 会有 I/O 开销。使用有界单请求大小和总 spool 配额；采集错误、超额、磁盘写失败均优先保住 API 转发并输出结构化可检索日志/计数。达到限额停止采集后续正文，尽可能补不完整 end；若连 end 都无法写入，则启动恢复时补标记。磁盘满/丢失持久卷/宿主机突然掉电不能保证零丢失，不能承诺 fail-open 和绝对无损同时成立。进程退出在 HTTP drain 后停止后台上传；剩余文件下次启动继续。

## 配置

环境变量统一以 `TRACE_ARCHIVE_` 开头：

| 名称 | 默认 / 说明 |
| --- | --- |
| ENABLED | false |
| SPOOL_DIR | ./data/agent-traces（持久卷，权限 0700，文件 0600） |
| BUCKET | 启用时必填 |
| PREFIX | raw/v1 |
| REGION | us-east-1 |
| ENDPOINT | 空：AWS 区域 S3 endpoint；非空：HTTPS S3 兼容 endpoint，不带 bucket/query/凭据 |
| ACCESS_KEY_ID | 启用时必填，独立归档 AK |
| SECRET_ACCESS_KEY | 启用时必填，独立归档 SK |
| SESSION_TOKEN | 可选临时凭据 token（轮换后需重启） |
| WORKERS | 2，范围 1–16 |
| MAX_REQUEST_MB | 64，单请求 JSONL 大小上限 |
| MAX_SPOOL_MB | 5120，至少为 MAX_REQUEST_MB 的 8 倍；正文采集使用前 75%，余量留给 end 和压缩；压缩串行，上传仍并发 |
| SCAN_SECONDS | 15 |
| UPLOAD_TIMEOUT_SECONDS | 120 |

采用现有 AWS SDK v2 的 SigV4 signer 对 path-style PUT 签名，不引入新的依赖。对压缩文件计算 SHA-256，签名 payload，发送 checksum 供 S3 校验。TLS 验证开启；上传不跟随重定向，防止凭据被发向非配置目的地。后台上传支持重试和有限并发，不打印认证信息/正文。启用但配置错误时启动失败，避免误以为归档已开启。

当前文件持久性方案面向 Linux/macOS 本地持久文件系统，不应使用不支持可靠 flock、原子 rename、fsync 的网络挂载。配额统计文件逻辑字节数，不包含文件系统块、目录及其他进程占用；持久卷实际空间应大于此配额，并监控剩余空间。单请求限制针对 Base64 后的 JSONL（约为原始正文的 4/3 加事件开销），不是原始 token 大小。

### 启用示例（生产操作，实施时另行配置）

```dotenv
TRACE_ARCHIVE_ENABLED=true
TRACE_ARCHIVE_SPOOL_DIR=/data/agent-traces
TRACE_ARCHIVE_BUCKET=your-private-trace-bucket
TRACE_ARCHIVE_PREFIX=raw/v1
TRACE_ARCHIVE_REGION=ap-northeast-1
TRACE_ARCHIVE_ACCESS_KEY_ID=<从密钥管理系统注入>
TRACE_ARCHIVE_SECRET_ACCESS_KEY=<从密钥管理系统注入>
```

AWS S3 凭据至少授予目标前缀的 `s3:PutObject`，不需要公网读权限；若 bucket 默认使用 SSE-KMS，按该 KMS key 的要求配置加密权限。S3 兼容服务需支持 path-style PUT、SigV4 和 SHA-256 checksum。通过 bucket 默认加密管理加密策略。本实现不管理 bucket、KMS、生命周期规则或密钥自动刷新。

客户端同一真实会话的每次请求发送 `X-EMO-Session-ID: <stable-session-id>`。没有此头则原始正文仍保存，但不保证能够重建完整 session；例如客户端仅发送 previous_response_id 或增量上下文，后续清洗必须解析这些关联，而不能把每个请求误当作完整对话。HTTP 响应正文里的工具调用/工具返回如实保留，未经过网关的本地工具过程不会自动出现。

运维检索日志中的 `trace_archive`：`capture_skipped`、`capture_truncated`、`capture_write_failed`、`capture_finalize_failed`、`recovery_failed`、`spool_scan_failed`、`upload_failed`。日志限频，累计失败数不会丢；进程退出打印汇总统计。本版尚未接入外部监控告警，需要部署侧对错误和磁盘积压配置告警。

### 代码位置

- `pkg/tracearchive/`：配置、JSONL 采集、本地恢复、gzip 队列、S3 签名上传。
- `middleware/trace_archive.go`：鉴权后透明读写采集。
- `router/relay-router.go` / `main.go`：路由挂载、启停。
- `model/log.go` / `relay/helper/stream_scanner.go`：只附带已有用量与流状态，不改计费计算。
- `.env.example`：默认关闭的配置模板。

## 离线训练数据构建

S3 中的 `raw/v1` 是唯一事实源，不为了迎合某个外部数据集而覆盖或改写。仓库提供独立的离线构建命令，将下载到本地的 EMO raw 生成两种由我们自行定义、可版本演进的数据：

```text
raw/v1/*.jsonl.gz
  -> emo-agent-exchange/1  每个 HTTP 请求一条，保留客户端请求/响应和执行元数据
  -> emo-agent-session/1   按已知 user + session 合并的标准 messages/tools 轨迹
```

两个参考样本只用于确认训练通常需要完整 messages、tool calls、usage、状态和时间等信息，不作为目标 schema，不复制其命名，也不要求与其目录或字段兼容。构建器只读取本系统 `tracearchive` 产生的 JSONL v1。

exchange 与 session 中的 user_id、session_id 使用部署方持有的 HMAC-SHA256 identity key 生成稳定假名，原始 ID 不进入训练文件。identity key 至少 32 字节，必须放在密钥管理系统或权限为 0600 的独立文件中，不能写入仓库。请求 JSON 中常见的认证、cookie、password、secret 字段会做结构化遮盖；这不是完整的隐私清洗，prompt、代码和工具输出仍可能包含敏感内容，进入训练前还必须执行授权筛选、DLP、质量过滤、去重以及按用户隔离 train/validation/test。

只有带稳定 session ID、采集完整、请求成功、响应有有效内容并且协议可标准化的 exchange 才进入 session 数据。unknown session 仍保留在 exchange 数据中，但不会把多个无关请求猜成同一会话。失败、取消、截断和无法标准化的数据保留在 exchange 层用于审计或后续改进，不会静默伪造成成功训练样本。

示例命令：

```sh
go run ./cmd/trace-dataset \
  -input /secure/downloaded-raw-v1 \
  -exchanges /secure/output/exchanges.jsonl.zst \
  -sessions /secure/output/sessions.jsonl.zst \
  -identity-key-file /secure/secrets/trace-dataset-hmac.key
```

输出采用 JSONL + Zstandard，先写同目录临时文件，完整压缩并 fsync 后才原子替换目标文件；输入损坏时构建失败且不发布半成品。构建报告输出到 stdout。此命令当前处理本地文件，不给线上只写 IAM 用户增加 S3 读取权限，也不会自动从生产 bucket 下载数据。

相关代码：

- `pkg/tracedataset/`：raw 校验、exchange 标准化、session 合并、脱敏和原子 zstd 输出。
- `cmd/trace-dataset/`：离线命令入口。

## 验收与部署

- 单测验证字节级 JSON/SSE/二进制还原、seq 顺序、未知 session、路径安全、请求及 spool 限额、上传失败保留和固定 key 重试、进程中断恢复、压缩 checksum。
- 中间件测试验证 API 返回内容/状态/Flush 不变，WriteString 被采集，未鉴权/关闭时不采集，读写错误不影响转发，取消和 panic 标记，计费汇总可关联。
- 编译相关包及主程序；本次只使用本地伪 S3 接收端，不发真实推理请求、不上传真实用户数据。
- 生产启用前配置独立私有 bucket/凭据和持久卷，先用测试账号发一条非流式和一条 SSE 请求，核对 S3 对象解压结果、终止标记及正常计费，再开放实际采集。
- 训练数据清洗/授权筛选/去重/按用户划分训练验证集为后续独立流程，raw 不覆盖重写成清洗数据。

### 本地验证结果

2026-09-08 已验证：

```sh
go test ./pkg/tracearchive ./middleware ./model ./relay/helper
go test -race ./pkg/tracedataset
go test -race ./pkg/tracearchive ./middleware -run 'Test(Archive|Crash|Workers|Configuration|S3|RawTrace)' -count=1
go build -o /tmp/trace-dataset ./cmd/trace-dataset
go build -o /tmp/emo-api .
```

均通过。测试包含本地 HTTPS 模拟 S3，检查签名头、正文校验和、不跟随重定向、上传失败重启后使用相同对象 key；以及二进制/JSON/SSE 原文还原、限额、半行恢复、不重复 end、实际计费用量挂载、读写异常和本地存储不可用时保持 API 行为。macOS 竞态测试链接器出现 LC_DYSYMTAB 警告，但测试通过且未报告数据竞态。

尚未进行真实 bucket 联调、生产负载测试或启用线上采集；当前配置仍默认关闭。这里的验证不等于已经完成真实 S3 备份，也不意味着所有原始数据可以直接用于训练。

## 已创建的 AWS 资源（2026-09-08）

已在 AWS 账号 `477075396603` 创建：

| 资源 | 配置 |
| --- | --- |
| S3 bucket | `semo-agent-traces-477075396603-apne1` |
| Region | `ap-northeast-1`（东京） |
| Bucket prefix | `raw/v1` |
| IAM 用户 | `arn:aws:iam::477075396603:user/emo-api-trace-archive` |
| IAM 内联策略 | `WriteRawAgentTraces` |

Bucket 已开启版本控制、SSE-S3（AES-256）默认加密、完整 Public Access Block，并通过 bucket policy 拒绝非 HTTPS 请求。IAM 用户只允许对 `arn:aws:s3:::semo-agent-traces-477075396603-apne1/raw/v1/*` 执行 `s3:PutObject`，不具备 List/Get/Delete，也不能写入其他前缀。

专用静态凭据保存在本机 `/Users/fei.xia/Downloads/emo-api-trace-archive_accessKeys.csv`，权限为 `0600`；该文件不在 Git 仓库中。部署时将其中两列分别注入 `TRACE_ARCHIVE_ACCESS_KEY_ID` 和 `TRACE_ARCHIVE_SECRET_ACCESS_KEY`，不要把 CSV 上传到服务器目录或提交到 Git。生产环境若提供密钥管理器，应将 Secret 复制到密钥管理器后删除本地 CSV；当前实现读取环境变量，轮换 Key 后需要重启服务。

生产环境参数：

```dotenv
TRACE_ARCHIVE_ENABLED=true
TRACE_ARCHIVE_SPOOL_DIR=/data/agent-traces
TRACE_ARCHIVE_BUCKET=semo-agent-traces-477075396603-apne1
TRACE_ARCHIVE_PREFIX=raw/v1
TRACE_ARCHIVE_REGION=ap-northeast-1
TRACE_ARCHIVE_ACCESS_KEY_ID=<从密钥管理器注入>
TRACE_ARCHIVE_SECRET_ACCESS_KEY=<从密钥管理器注入>
```

真实权限联调已经完成：专用 Key 成功 Put 一个零字节测试对象，S3 返回 `AES256` 和 VersionId；随后用管理员身份删除该精确测试版本。另已实际确认 List、Get、Delete 和前缀外 Put 均被拒绝。Bucket 当前没有遗留的权限测试对象。线上开启仍需部署侧提供独立持久卷，并完成一条非流式和一条 SSE 的端到端归档验收。
